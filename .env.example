PORT=3000
SESSION_SECRET=change-this-to-a-random-secret-in-production

# Email (for sending ticket PDFs)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

# WhatsApp (uses wa.me links by default; no API credentials required)
# The app will redirect users to WhatsApp Web/mobile with a prefilled message.
# If you host generated PDFs publicly, set PUBLIC_BASE_URL and MEDIA_TOKEN so links can be included in messages.
# Example: PUBLIC_BASE_URL=https://yourdomain.com MEDIA_TOKEN=secret-token
